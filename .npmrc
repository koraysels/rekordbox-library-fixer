# Hoist all packages to root node_modules — required for Electron native module loading
# and electron-builder packaging to work correctly with pnpm's symlink structure
shamefully-hoist=true
strict-peer-dependencies=false
